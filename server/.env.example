# 复制为 .env 并按需修改
PORT=3000

# 客户端标识
APP_ID=demo-client

# 时间戳允许偏差(毫秒),默认 5 分钟
TIMESTAMP_TOLERANCE_MS=300000

# nonce 缓存有效期(毫秒),默认 5 分钟
NONCE_TTL_MS=300000

# RSA 公钥下发接口是否返回完整公钥文本
PUBKEY_ENDPOINT_ENABLED=true
